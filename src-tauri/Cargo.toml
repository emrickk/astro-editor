[package]
name = "astro-editor"
version = "1.0.17"
description = "A Tauri App"
authors = ["you"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "astro_editor_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["macos-private-api", "protocol-asset"] }

# Type-safe Tauri commands with auto-generated TypeScript bindings
specta = { version = "=2.0.0-rc.22", features = ["indexmap", "serde_json"] }
specta-typescript = "0.0.9"
tauri-specta = { version = "=2.0.0-rc.21", features = ["derive", "typescript"] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_norway = "0.9.42"
tokio = { version = "1", features = ["full"] }
notify = "9.0.0-rc.2"
walkdir = "2"
rfd = "0.17"
regex = "1.10"
tauri-plugin-clipboard-manager = "2.3.0"
tauri-plugin-fs = "2.4.0"
tauri-plugin-dialog = "2.3.0"
tauri-plugin-process = "2.3.0"
tauri-plugin-log = "2.0.0"
log = "0.4"
chrono = "0.4"
swc_common = "21"
swc_ecma_ast = "23"
swc_ecma_parser = "39"
swc_ecma_visit = "23"
dirs = "6"
indexmap = { version = "2", features = ["serde"] }
pathdiff = "0.2"
reqwest = { version = "0.13", features = ["json", "stream"] }
rustix = { version = "1.1.4", features = ["fs"] }
uuid = { version = "1.23", features = ["v4"] }
tauri-plugin-os = "2.3.2"
tauri-plugin-window-state = "2"
tauri-plugin-deep-link = "2"
tauri-plugin-single-instance = { version = "2", features = ["deep-link"] }

[dev-dependencies]
tempfile = "3.27"

# macOS-only dependencies
[target.'cfg(target_os = "macos")'.dependencies]
tauri = { version = "2", features = ["macos-private-api"] }
# Pinned to 0.6 to match tauri's internal version. Having both 0.7 and 0.6 in
# the tree causes an LTO linker symbol conflict on
# `__CLASS_NSVisualEffectViewTagged` (call site: `apply_vibrancy` in src/lib.rs).
# Re-tested against tauri 2.11.2 (May 2026) — conflict still recurs, tauri still
# bundles 0.6 internally. Do not bump without verifying the LTO issue is resolved.
window-vibrancy = "0.6"

# Desktop-only dependencies (excludes Android and iOS)
[target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dependencies]
tauri-plugin-updater = "2"

[profile.release]
codegen-units = 1        # Better LLVM optimization
lto = true               # Link-time optimizations
opt-level = "s"          # Prioritize small binary size
panic = "abort"          # Disable panic handlers
strip = true             # Remove debug symbols
